CREATE TABLE singleCol(
        ID serial primary key,
        col1 varchar
        col2 varchar
);

CREATE TABLE doubleCol (
        ID serial primary key,,
        keyName varchar
        valName varchar
);

CREATE TABLE tripleCol (
        ID serial primary key,
        keyName varchar
        valName1 varchar
        valName2 varchar
);